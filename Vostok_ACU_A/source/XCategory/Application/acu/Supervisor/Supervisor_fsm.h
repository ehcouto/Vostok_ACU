/**
 * @brief
 *            The StateMachineCodeGenerator finite state machine auto generated declarations and definitions for the
 *            PlantUML state machine diagram Supervisor State Machine.
 *
 * @details
 *            DO NOT edit this file or the state machine will not execute as defined in the
 *            Supervisor State Machine diagram.
 *
 *            This file must be included in the file the Supervisor State Machine was defined in.
 *
 *            File Path: C:\Data\Projects\NUCLEUS_XCATEGORY\SOURCE\APPLICATION\APPLICATION_CORE_ACU\ApplicationAcu\Supervisor\Supervisor.c
 *            PlantUML Diagram: Supervisor State Machine
 *            StateMachineCodeGenerator Version: 14.7.1-alpha
 *            Created on: 2023y9m28d 11h1m46s
 *
 * @copyright Copyright (c) 2023 WhirlpoolCorporation
 */
//-------------------------------------------------------------------------------------------------------------------
//-------------------------------------------------------------------------------------------------------------------
#ifndef SUPERVISOR_FSM_H_
#define SUPERVISOR_FSM_H_

//===================================================================================================================
// Private Declarations.
//===================================================================================================================

/* 
 * Macro is intended to be used in the customization functions for dispatching events to the state machine.
 * This macro should be used instead of the SupervisorEvent() function due to the Key Statements
 *     'state variable =
 * and
 *     'developer variables =
 * These Key Statements directly affect the function arguments of the customization functions and using the macro
 * DISPATCH_FSM_EVENT(event) will abstract the function arguments, allowing the developer to not have to modify code when the
 * Key Statements are removed/added to the PlantUML text.
 *
 * @param event = SUPERVISOR_FSM_EVENT_TYPE
 */
#define DISPATCH_FSM_EVENT(event)\
    SupervisorFsmEvent(event, fsm_var, okay_to_start_cycle)

/*
 * Macro is used to define the function arguments to the state machine customization functions.
 * The arguments are defined as a macro to abstract the state machine variables and developer variables
 * from the customization functions headers added to the module.c file.  This was done to prevent duplicate
 * customization functions from being defined in the module.c file when the developer adds or modifies the
 * Key Statements 'state variable = OR 'developer variables =.
 */
#define FSM_CUSTOMIZATION_FUNCTION_ARGUMENT\
    SUPERVISOR_FSM_VAR_TYPE * fsm_var /* DO NOT USE, used by the auto-generated code for controlling execution of the state machine */,\
    BOOL_TYPE * okay_to_start_cycle /* key statement 'developer variables = */

/*
 * Macro is used to define the function arguments in a call to a finite state machine customization function.
 * The macro should be used instead of directly defining the function arguments due to the Key Statements
 *     'state variable =
 * and
 *     'developer variables =
 * These Key Statements directly affect the function arguments of the customization functions and using the macro
 * will abstract the function arguments, allowing the developer to not have to modify code when the Key Statements are
 * removed/added to the PlantUML text.
 */
#define FSM_PASS_ARGUMENTS\
    fsm_var, okay_to_start_cycle


/*
 * Macro is used to define the function arguments to the state machine guard functions.
 * The arguments are defined as a macro to abstract the developer variables. This was
 * done to prevent duplicate guard functions from being defined in the module.c file
 * when the developer adds or modifies the Key Statement
 *     'developer variables =.
 */
#define FSM_GUARD_FUNCTION_ARGUMENT\
    BOOL_TYPE * okay_to_start_cycle /* key statement 'developer variables = */

// Supervisor state diagram states
typedef enum
{
    FSM_STATE_NONE,
    FSM_STATE_CANCELLING, // PlantUML diagram state CANCELLING
    FSM_STATE_DELAY, // PlantUML diagram state DELAY
    FSM_STATE_DELAY_PAUSE, // PlantUML diagram state DELAY_PAUSE
    FSM_STATE_END_OF_CYCLE, // PlantUML diagram state END_OF_CYCLE
    FSM_STATE_FAULT, // PlantUML diagram state FAULT
    FSM_STATE_PAUSE, // PlantUML diagram state PAUSE
    FSM_STATE_PROGRAMMING, // PlantUML diagram state PROGRAMMING
    FSM_STATE_PAUSE_CANCELLING, // PlantUML diagram state PauseCancelling
    FSM_STATE_RUN // PlantUML diagram state RUN
} SUPERVISOR_FSM_STATE_TYPE;
// Defines the total number of enumerated values in the SUPERVISOR_FSM_STATE_TYPE enumeration.
#define NUM_OF_FSM_STATES 10

// declaration of the structure that will be passed as a function argument to the state machine auto generated functions
typedef struct
{
    SUPERVISOR_FSM_STATE_TYPE State; // the current state of the state machine
    BOOL_TYPE Transition; // TRUE event triggered a state transition else FALSE
} SUPERVISOR_FSM_VAR_TYPE;

/*
 * Supervisor state diagram events.
 * The lower byte is used for defining state diagram events.
 * The upper byte is used for defining external modules.  External modules used when events from another modules API are defined in the state diagram.
 * Format for defining an external module event in the state diagram: module_name = module_event
 * To dispatch the external module event to the state machine: DISPATCH_FSM_EVENT(mdoule_name + module_event)
 */
typedef enum
{
    // private events
    FSM_EVENT_DELAY = 1, // PlantUML diagram event evDelay
    FSM_EVENT_END, // PlantUML diagram event evEnd
    FSM_EVENT_FAULT, // PlantUML diagram event evFault
    FSM_EVENT_FAULT_RESET, // PlantUML diagram event evFaultReset
    FSM_EVENT_PAUSE, // PlantUML diagram event evPause
    FSM_EVENT_RESET, // PlantUML diagram event evReset
    FSM_EVENT_RESUME, // PlantUML diagram event evResume
    FSM_EVENT_START // PlantUML diagram event evStart
} SUPERVISOR_FSM_EVENT_TYPE;

// Supervisor State Machine state machine control functions
static void SupervisorFsmInitialize(SUPERVISOR_FSM_STATE_TYPE * state, BOOL_TYPE * okay_to_start_cycle);
static void SupervisorFsmExecute(SUPERVISOR_FSM_STATE_TYPE * state, BOOL_TYPE * okay_to_start_cycle);
static void SupervisorFsmTerminate(SUPERVISOR_FSM_STATE_TYPE * state, BOOL_TYPE * okay_to_start_cycle);
static void SupervisorFsmEvent(SUPERVISOR_FSM_EVENT_TYPE event, FSM_CUSTOMIZATION_FUNCTION_ARGUMENT);
static SUPERVISOR_FSM_VAR_TYPE SupervisorBuildFsmVar(SUPERVISOR_FSM_STATE_TYPE state);

// Supervisor State Machine state machine customization function prototypes
static void CancellingEntry(FSM_CUSTOMIZATION_FUNCTION_ARGUMENT);
static void CancellingExit(FSM_CUSTOMIZATION_FUNCTION_ARGUMENT);
static void DelayDo(FSM_CUSTOMIZATION_FUNCTION_ARGUMENT);
static void DelayEntry(FSM_CUSTOMIZATION_FUNCTION_ARGUMENT);
static void DelayPauseDo(FSM_CUSTOMIZATION_FUNCTION_ARGUMENT);
static void DelayPauseEntry(FSM_CUSTOMIZATION_FUNCTION_ARGUMENT);
static void DelayPauseExit(FSM_CUSTOMIZATION_FUNCTION_ARGUMENT);
static void EndEntry(FSM_CUSTOMIZATION_FUNCTION_ARGUMENT);
static void FaultDo(FSM_CUSTOMIZATION_FUNCTION_ARGUMENT);
static void FaultEntry(FSM_CUSTOMIZATION_FUNCTION_ARGUMENT);
static BOOL_TYPE IsCancelCycleEnabled(FSM_GUARD_FUNCTION_ARGUMENT);
static BOOL_TYPE IsCancellingToFaultEnabled(FSM_GUARD_FUNCTION_ARGUMENT);
static void PauseDo(FSM_CUSTOMIZATION_FUNCTION_ARGUMENT);
static void PauseEntry(FSM_CUSTOMIZATION_FUNCTION_ARGUMENT);
static void PauseExit(FSM_CUSTOMIZATION_FUNCTION_ARGUMENT);
static void ProgrammingDo(FSM_CUSTOMIZATION_FUNCTION_ARGUMENT);
static void ProgrammingEntry(FSM_CUSTOMIZATION_FUNCTION_ARGUMENT);
static void Reset(FSM_CUSTOMIZATION_FUNCTION_ARGUMENT);
static void ResetTasks(FSM_CUSTOMIZATION_FUNCTION_ARGUMENT);
static void Resume(FSM_CUSTOMIZATION_FUNCTION_ARGUMENT);
static void RunDo(FSM_CUSTOMIZATION_FUNCTION_ARGUMENT);
static void RunEntry(FSM_CUSTOMIZATION_FUNCTION_ARGUMENT);
static void RunExit(FSM_CUSTOMIZATION_FUNCTION_ARGUMENT);
static void SetSystemStateCancelling(FSM_CUSTOMIZATION_FUNCTION_ARGUMENT);
static void SetSystemStateDelay(FSM_CUSTOMIZATION_FUNCTION_ARGUMENT);
static void SetSystemStateDelayPause(FSM_CUSTOMIZATION_FUNCTION_ARGUMENT);
static void SetSystemStateEndOfCycle(FSM_CUSTOMIZATION_FUNCTION_ARGUMENT);
static void SetSystemStateFault(FSM_CUSTOMIZATION_FUNCTION_ARGUMENT);
static void SetSystemStatePause(FSM_CUSTOMIZATION_FUNCTION_ARGUMENT);
static void SetSystemStateProgramming(FSM_CUSTOMIZATION_FUNCTION_ARGUMENT);
static void SetSystemStateRun(FSM_CUSTOMIZATION_FUNCTION_ARGUMENT);
static void Start(FSM_CUSTOMIZATION_FUNCTION_ARGUMENT);
static void StartCancellingCycle(FSM_CUSTOMIZATION_FUNCTION_ARGUMENT);
static void UpdateOkayToStartCycle(FSM_CUSTOMIZATION_FUNCTION_ARGUMENT);

//===================================================================================================================
// Function definitions.
//===================================================================================================================
/**
 * @brief Configures the Supervisor state machine to start execution from the Pseudo Initial state.
 * @param state: pointer to the state machine state variable
 * @param BOOL_TYPE * okay_to_start_cycle: key statement 'developer variables =
 */
static void SupervisorFsmInitialize(SUPERVISOR_FSM_STATE_TYPE * state, BOOL_TYPE * okay_to_start_cycle)
{
    // [*] -->PROGRAMMING
    SUPERVISOR_FSM_VAR_TYPE fsm_var = SupervisorBuildFsmVar(FSM_STATE_PROGRAMMING);
    // PROGRAMMING :entry/   ProgrammingEntry    SetSystemStateProgramming
    ProgrammingEntry(&fsm_var, okay_to_start_cycle);
    if ( fsm_var.Transition == FALSE )
    {
        SetSystemStateProgramming(&fsm_var, okay_to_start_cycle);
    }
    *state = fsm_var.State;
}

/**
 * @brief Executes the Supervisor state machine do actions.
 * @param state: pointer to the state machine state variable
 * @param BOOL_TYPE * okay_to_start_cycle: key statement 'developer variables =
 */
static void SupervisorFsmExecute(SUPERVISOR_FSM_STATE_TYPE * state, BOOL_TYPE * okay_to_start_cycle)
{
    SUPERVISOR_FSM_VAR_TYPE fsm_var = SupervisorBuildFsmVar(*state);
    switch ( fsm_var.State )
    {
        default:
            // do nothing
            break;
        case FSM_STATE_CANCELLING:
            // CANCELLING :do/   RunDo    SetSystemStateCancelling
            RunDo(&fsm_var, okay_to_start_cycle);
            if ( fsm_var.Transition == FALSE )
            {
                SetSystemStateCancelling(&fsm_var, okay_to_start_cycle);
            }
            break;
        case FSM_STATE_DELAY:
            // DELAY :do/   UpdateOkayToStartCycle    DelayDo    SetSystemStateDelay
            UpdateOkayToStartCycle(&fsm_var, okay_to_start_cycle);
            if ( fsm_var.Transition == FALSE )
            {
                DelayDo(&fsm_var, okay_to_start_cycle);
                if ( fsm_var.Transition == FALSE )
                {
                    SetSystemStateDelay(&fsm_var, okay_to_start_cycle);
                }
            }
            break;
        case FSM_STATE_DELAY_PAUSE:
            // DELAY_PAUSE :do/   UpdateOkayToStartCycle    DelayPauseDo    SetSystemStateDelayPause
            UpdateOkayToStartCycle(&fsm_var, okay_to_start_cycle);
            if ( fsm_var.Transition == FALSE )
            {
                DelayPauseDo(&fsm_var, okay_to_start_cycle);
                if ( fsm_var.Transition == FALSE )
                {
                    SetSystemStateDelayPause(&fsm_var, okay_to_start_cycle);
                }
            }
            break;
        case FSM_STATE_END_OF_CYCLE:
            // END_OF_CYCLE :do/   SetSystemStateEndOfCycle
            SetSystemStateEndOfCycle(&fsm_var, okay_to_start_cycle);
            break;
        case FSM_STATE_FAULT:
            // FAULT :do/   FaultDo    SetSystemStateFault
            FaultDo(&fsm_var, okay_to_start_cycle);
            if ( fsm_var.Transition == FALSE )
            {
                SetSystemStateFault(&fsm_var, okay_to_start_cycle);
            }
            break;
        case FSM_STATE_PAUSE:
            // PAUSE :do/   UpdateOkayToStartCycle    PauseDo    SetSystemStatePause
            UpdateOkayToStartCycle(&fsm_var, okay_to_start_cycle);
            if ( fsm_var.Transition == FALSE )
            {
                PauseDo(&fsm_var, okay_to_start_cycle);
                if ( fsm_var.Transition == FALSE )
                {
                    SetSystemStatePause(&fsm_var, okay_to_start_cycle);
                }
            }
            break;
        case FSM_STATE_PROGRAMMING:
            // PROGRAMMING :do/   UpdateOkayToStartCycle    ProgrammingDo    SetSystemStateProgramming
            UpdateOkayToStartCycle(&fsm_var, okay_to_start_cycle);
            if ( fsm_var.Transition == FALSE )
            {
                ProgrammingDo(&fsm_var, okay_to_start_cycle);
                if ( fsm_var.Transition == FALSE )
                {
                    SetSystemStateProgramming(&fsm_var, okay_to_start_cycle);
                }
            }
            break;
        case FSM_STATE_PAUSE_CANCELLING:
            // PauseCancelling :do/   UpdateOkayToStartCycle    PauseDo    SetSystemStatePause
            UpdateOkayToStartCycle(&fsm_var, okay_to_start_cycle);
            if ( fsm_var.Transition == FALSE )
            {
                PauseDo(&fsm_var, okay_to_start_cycle);
                if ( fsm_var.Transition == FALSE )
                {
                    SetSystemStatePause(&fsm_var, okay_to_start_cycle);
                }
            }
            break;
        case FSM_STATE_RUN:
            // RUN :do/   RunDo    SetSystemStateRun
            RunDo(&fsm_var, okay_to_start_cycle);
            if ( fsm_var.Transition == FALSE )
            {
                SetSystemStateRun(&fsm_var, okay_to_start_cycle);
            }
            break;
    }
    *state = fsm_var.State;
}

/**
 * @brief Terminates the Supervisor state machine.
 * @param state: pointer to the state machine state variable
 * @param BOOL_TYPE * okay_to_start_cycle: key statement 'developer variables =
 */
static void SupervisorFsmTerminate(SUPERVISOR_FSM_STATE_TYPE * state, BOOL_TYPE * okay_to_start_cycle)
{
    SUPERVISOR_FSM_VAR_TYPE fsm_var = SupervisorBuildFsmVar(*state);
    switch ( fsm_var.State )
    {
        default:
            // do nothing
            break;
        case FSM_STATE_CANCELLING:
            // CANCELLING :exit/   CancellingExit
            CancellingExit(&fsm_var, okay_to_start_cycle);
            break;
        case FSM_STATE_DELAY_PAUSE:
            // DELAY_PAUSE :exit /   DelayPauseExit
            DelayPauseExit(&fsm_var, okay_to_start_cycle);
            break;
        case FSM_STATE_PAUSE:
            // PAUSE :exit/   PauseExit
            PauseExit(&fsm_var, okay_to_start_cycle);
            break;
        case FSM_STATE_PAUSE_CANCELLING:
            // PauseCancelling :exit /   PauseExit
            PauseExit(&fsm_var, okay_to_start_cycle);
            break;
        case FSM_STATE_RUN:
            // RUN :exit/   RunExit
            RunExit(&fsm_var, okay_to_start_cycle);
            break;
    }
    *state = FSM_STATE_NONE;
}

/**
 * NOTE: The DISPATCH_FSM_EVENT(event) macro should be used instead of this function to dispatch events to the
 *       state machine to make maintaining and updating the state machine customization functions easier.
 *
 * @brief Dispatch the event to the Supervisor state machine and consume it.
 * @param event: the event to dispatch to the state machine
 */
static void SupervisorFsmEvent(SUPERVISOR_FSM_EVENT_TYPE event, FSM_CUSTOMIZATION_FUNCTION_ARGUMENT)
{
    fsm_var->Transition = FALSE;
    switch ( event )
    {
        default:
            // do nothing
            break;
        case FSM_EVENT_DELAY: // event evDelay
            switch ( fsm_var->State )
            {
                default:
                    // do nothing
                    break;
                case FSM_STATE_PROGRAMMING:
                    {
                        // PROGRAMMING --> DELAY : evDelay

                        // define state being transitioned to
                        fsm_var->State = FSM_STATE_DELAY;
                        // DELAY :entry/   DelayEntry    SetSystemStateDelay
                        DelayEntry(fsm_var, okay_to_start_cycle);
                        if ( fsm_var->Transition == FALSE )
                        {
                            SetSystemStateDelay(fsm_var, okay_to_start_cycle);
                        }
                        fsm_var->Transition = TRUE;
                    }
                    break;
            }
            break;
        case FSM_EVENT_END: // event evEnd
            switch ( fsm_var->State )
            {
                default:
                    // do nothing
                    break;
                case FSM_STATE_CANCELLING:
                    {
                        // CANCELLING -->PROGRAMMING: evEnd /  Reset   ResetTasks

                        // CANCELLING :exit/   CancellingExit
                        CancellingExit(fsm_var, okay_to_start_cycle);
                        // transition action
                        Reset(fsm_var, okay_to_start_cycle);
                        ResetTasks(fsm_var, okay_to_start_cycle);
                        // define state being transitioned to
                        fsm_var->State = FSM_STATE_PROGRAMMING;
                        // PROGRAMMING :entry/   ProgrammingEntry    SetSystemStateProgramming
                        ProgrammingEntry(fsm_var, okay_to_start_cycle);
                        if ( fsm_var->Transition == FALSE )
                        {
                            SetSystemStateProgramming(fsm_var, okay_to_start_cycle);
                        }
                        fsm_var->Transition = TRUE;
                    }
                    break;
                case FSM_STATE_RUN:
                    {
                        // RUN --> END_OF_CYCLE : evEnd

                        // RUN :exit/   RunExit
                        RunExit(fsm_var, okay_to_start_cycle);
                        // define state being transitioned to
                        fsm_var->State = FSM_STATE_END_OF_CYCLE;
                        // END_OF_CYCLE :entry/   EndEntry    SetSystemStateEndOfCycle
                        EndEntry(fsm_var, okay_to_start_cycle);
                        if ( fsm_var->Transition == FALSE )
                        {
                            SetSystemStateEndOfCycle(fsm_var, okay_to_start_cycle);
                        }
                        fsm_var->Transition = TRUE;
                    }
                    break;
            }
            break;
        case FSM_EVENT_FAULT: // event evFault
            switch ( fsm_var->State )
            {
                default:
                    // do nothing
                    break;
                case FSM_STATE_CANCELLING:
                    {
                        // CANCELLING --> FAULT : evFault

                        // CANCELLING :exit/   CancellingExit
                        CancellingExit(fsm_var, okay_to_start_cycle);
                        // define state being transitioned to
                        fsm_var->State = FSM_STATE_FAULT;
                        // FAULT :entry/   FaultEntry    SetSystemStateFault
                        FaultEntry(fsm_var, okay_to_start_cycle);
                        if ( fsm_var->Transition == FALSE )
                        {
                            SetSystemStateFault(fsm_var, okay_to_start_cycle);
                        }
                        fsm_var->Transition = TRUE;
                    }
                    break;
                case FSM_STATE_DELAY:
                    {
                        // DELAY --> FAULT : evFault

                        // define state being transitioned to
                        fsm_var->State = FSM_STATE_FAULT;
                        // FAULT :entry/   FaultEntry    SetSystemStateFault
                        FaultEntry(fsm_var, okay_to_start_cycle);
                        if ( fsm_var->Transition == FALSE )
                        {
                            SetSystemStateFault(fsm_var, okay_to_start_cycle);
                        }
                        fsm_var->Transition = TRUE;
                    }
                    break;
                case FSM_STATE_DELAY_PAUSE:
                    {
                        // DELAY_PAUSE --> FAULT : evFault

                        // DELAY_PAUSE :exit /   DelayPauseExit
                        DelayPauseExit(fsm_var, okay_to_start_cycle);
                        // define state being transitioned to
                        fsm_var->State = FSM_STATE_FAULT;
                        // FAULT :entry/   FaultEntry    SetSystemStateFault
                        FaultEntry(fsm_var, okay_to_start_cycle);
                        if ( fsm_var->Transition == FALSE )
                        {
                            SetSystemStateFault(fsm_var, okay_to_start_cycle);
                        }
                        fsm_var->Transition = TRUE;
                    }
                    break;
                case FSM_STATE_END_OF_CYCLE:
                    {
                        // END_OF_CYCLE --> FAULT : evFault

                        // define state being transitioned to
                        fsm_var->State = FSM_STATE_FAULT;
                        // FAULT :entry/   FaultEntry    SetSystemStateFault
                        FaultEntry(fsm_var, okay_to_start_cycle);
                        if ( fsm_var->Transition == FALSE )
                        {
                            SetSystemStateFault(fsm_var, okay_to_start_cycle);
                        }
                        fsm_var->Transition = TRUE;
                    }
                    break;
                case FSM_STATE_FAULT:
                    {
                        // FAULT --> FAULT : evFault

                        // define state being transitioned to
                        fsm_var->State = FSM_STATE_FAULT;
                        // FAULT :entry/   FaultEntry    SetSystemStateFault
                        FaultEntry(fsm_var, okay_to_start_cycle);
                        if ( fsm_var->Transition == FALSE )
                        {
                            SetSystemStateFault(fsm_var, okay_to_start_cycle);
                        }
                        fsm_var->Transition = TRUE;
                    }
                    break;
                case FSM_STATE_PAUSE:
                    {
                        // PAUSE --> FAULT : evFault

                        // PAUSE :exit/   PauseExit
                        PauseExit(fsm_var, okay_to_start_cycle);
                        // define state being transitioned to
                        fsm_var->State = FSM_STATE_FAULT;
                        // FAULT :entry/   FaultEntry    SetSystemStateFault
                        FaultEntry(fsm_var, okay_to_start_cycle);
                        if ( fsm_var->Transition == FALSE )
                        {
                            SetSystemStateFault(fsm_var, okay_to_start_cycle);
                        }
                        fsm_var->Transition = TRUE;
                    }
                    break;
                case FSM_STATE_PROGRAMMING:
                    {
                        // PROGRAMMING --> FAULT : evFault

                        // define state being transitioned to
                        fsm_var->State = FSM_STATE_FAULT;
                        // FAULT :entry/   FaultEntry    SetSystemStateFault
                        FaultEntry(fsm_var, okay_to_start_cycle);
                        if ( fsm_var->Transition == FALSE )
                        {
                            SetSystemStateFault(fsm_var, okay_to_start_cycle);
                        }
                        fsm_var->Transition = TRUE;
                    }
                    break;
                case FSM_STATE_PAUSE_CANCELLING:
                    {
                        // PauseCancelling --> FAULT : evFault

                        // PauseCancelling :exit /   PauseExit
                        PauseExit(fsm_var, okay_to_start_cycle);
                        // define state being transitioned to
                        fsm_var->State = FSM_STATE_FAULT;
                        // FAULT :entry/   FaultEntry    SetSystemStateFault
                        FaultEntry(fsm_var, okay_to_start_cycle);
                        if ( fsm_var->Transition == FALSE )
                        {
                            SetSystemStateFault(fsm_var, okay_to_start_cycle);
                        }
                        fsm_var->Transition = TRUE;
                    }
                    break;
                case FSM_STATE_RUN:
                    {
                        // RUN --> FAULT : evFault

                        // RUN :exit/   RunExit
                        RunExit(fsm_var, okay_to_start_cycle);
                        // define state being transitioned to
                        fsm_var->State = FSM_STATE_FAULT;
                        // FAULT :entry/   FaultEntry    SetSystemStateFault
                        FaultEntry(fsm_var, okay_to_start_cycle);
                        if ( fsm_var->Transition == FALSE )
                        {
                            SetSystemStateFault(fsm_var, okay_to_start_cycle);
                        }
                        fsm_var->Transition = TRUE;
                    }
                    break;
            }
            break;
        case FSM_EVENT_FAULT_RESET: // event evFaultReset
            switch ( fsm_var->State )
            {
                default:
                    // do nothing
                    break;
                case FSM_STATE_FAULT:
                    {
                        if ( IsCancelCycleEnabled(okay_to_start_cycle) && IsCancellingToFaultEnabled(okay_to_start_cycle) )
                        {
                            // FAULT -->CANCELLING: evFaultReset   [ IsCancelCycleEnabled && IsCancellingToFaultEnabled ] /  StartCancellingCycle   ResetTasks

                            // transition action
                            StartCancellingCycle(fsm_var, okay_to_start_cycle);
                            ResetTasks(fsm_var, okay_to_start_cycle);
                            // define state being transitioned to
                            fsm_var->State = FSM_STATE_CANCELLING;
                            // CANCELLING :entry/   CancellingEntry    SetSystemStateCancelling
                            CancellingEntry(fsm_var, okay_to_start_cycle);
                            if ( fsm_var->Transition == FALSE )
                            {
                                SetSystemStateCancelling(fsm_var, okay_to_start_cycle);
                            }
                            fsm_var->Transition = TRUE;
                        }
                        else if ( !IsCancelCycleEnabled(okay_to_start_cycle) || !IsCancellingToFaultEnabled(okay_to_start_cycle) )
                        {
                            // FAULT -->PROGRAMMING: evFaultReset   [ !IsCancelCycleEnabled || !IsCancellingToFaultEnabled ]  / Reset

                            // transition action
                            Reset(fsm_var, okay_to_start_cycle);
                            // define state being transitioned to
                            fsm_var->State = FSM_STATE_PROGRAMMING;
                            // PROGRAMMING :entry/   ProgrammingEntry    SetSystemStateProgramming
                            ProgrammingEntry(fsm_var, okay_to_start_cycle);
                            if ( fsm_var->Transition == FALSE )
                            {
                                SetSystemStateProgramming(fsm_var, okay_to_start_cycle);
                            }
                            fsm_var->Transition = TRUE;
                        }
                    }
                    break;
            }
            break;
        case FSM_EVENT_PAUSE: // event evPause
            switch ( fsm_var->State )
            {
                default:
                    // do nothing
                    break;
                case FSM_STATE_CANCELLING:
                    {
                        // CANCELLING --> PauseCancelling : evPause

                        // CANCELLING :exit/   CancellingExit
                        CancellingExit(fsm_var, okay_to_start_cycle);
                        // define state being transitioned to
                        fsm_var->State = FSM_STATE_PAUSE_CANCELLING;
                        // PauseCancelling :entry/   PauseEntry    SetSystemStatePause
                        PauseEntry(fsm_var, okay_to_start_cycle);
                        if ( fsm_var->Transition == FALSE )
                        {
                            SetSystemStatePause(fsm_var, okay_to_start_cycle);
                        }
                        fsm_var->Transition = TRUE;
                    }
                    break;
                case FSM_STATE_DELAY:
                    {
                        // DELAY --> DELAY_PAUSE : evPause

                        // define state being transitioned to
                        fsm_var->State = FSM_STATE_DELAY_PAUSE;
                        // DELAY_PAUSE :entry/   DelayPauseEntry    SetSystemStateDelayPause
                        DelayPauseEntry(fsm_var, okay_to_start_cycle);
                        if ( fsm_var->Transition == FALSE )
                        {
                            SetSystemStateDelayPause(fsm_var, okay_to_start_cycle);
                        }
                        fsm_var->Transition = TRUE;
                    }
                    break;
                case FSM_STATE_RUN:
                    {
                        // RUN --> PAUSE : evPause

                        // RUN :exit/   RunExit
                        RunExit(fsm_var, okay_to_start_cycle);
                        // define state being transitioned to
                        fsm_var->State = FSM_STATE_PAUSE;
                        // PAUSE :entry/   PauseEntry    SetSystemStatePause
                        PauseEntry(fsm_var, okay_to_start_cycle);
                        if ( fsm_var->Transition == FALSE )
                        {
                            SetSystemStatePause(fsm_var, okay_to_start_cycle);
                        }
                        fsm_var->Transition = TRUE;
                    }
                    break;
            }
            break;
        case FSM_EVENT_RESET: // event evReset
            switch ( fsm_var->State )
            {
                default:
                    // do nothing
                    break;
                case FSM_STATE_CANCELLING:
                    {
                        // CANCELLING -->PROGRAMMING: evReset /  Reset   ResetTasks

                        // CANCELLING :exit/   CancellingExit
                        CancellingExit(fsm_var, okay_to_start_cycle);
                        // transition action
                        Reset(fsm_var, okay_to_start_cycle);
                        ResetTasks(fsm_var, okay_to_start_cycle);
                        // define state being transitioned to
                        fsm_var->State = FSM_STATE_PROGRAMMING;
                        // PROGRAMMING :entry/   ProgrammingEntry    SetSystemStateProgramming
                        ProgrammingEntry(fsm_var, okay_to_start_cycle);
                        if ( fsm_var->Transition == FALSE )
                        {
                            SetSystemStateProgramming(fsm_var, okay_to_start_cycle);
                        }
                        fsm_var->Transition = TRUE;
                    }
                    break;
                case FSM_STATE_DELAY:
                    {
                        // DELAY -->PROGRAMMING: evReset  / Reset

                        // transition action
                        Reset(fsm_var, okay_to_start_cycle);
                        // define state being transitioned to
                        fsm_var->State = FSM_STATE_PROGRAMMING;
                        // PROGRAMMING :entry/   ProgrammingEntry    SetSystemStateProgramming
                        ProgrammingEntry(fsm_var, okay_to_start_cycle);
                        if ( fsm_var->Transition == FALSE )
                        {
                            SetSystemStateProgramming(fsm_var, okay_to_start_cycle);
                        }
                        fsm_var->Transition = TRUE;
                    }
                    break;
                case FSM_STATE_DELAY_PAUSE:
                    {
                        // DELAY_PAUSE -->PROGRAMMING: evReset  / Reset

                        // DELAY_PAUSE :exit /   DelayPauseExit
                        DelayPauseExit(fsm_var, okay_to_start_cycle);
                        // transition action
                        Reset(fsm_var, okay_to_start_cycle);
                        // define state being transitioned to
                        fsm_var->State = FSM_STATE_PROGRAMMING;
                        // PROGRAMMING :entry/   ProgrammingEntry    SetSystemStateProgramming
                        ProgrammingEntry(fsm_var, okay_to_start_cycle);
                        if ( fsm_var->Transition == FALSE )
                        {
                            SetSystemStateProgramming(fsm_var, okay_to_start_cycle);
                        }
                        fsm_var->Transition = TRUE;
                    }
                    break;
                case FSM_STATE_END_OF_CYCLE:
                    {
                        // END_OF_CYCLE -->PROGRAMMING: evReset  / Reset

                        // transition action
                        Reset(fsm_var, okay_to_start_cycle);
                        // define state being transitioned to
                        fsm_var->State = FSM_STATE_PROGRAMMING;
                        // PROGRAMMING :entry/   ProgrammingEntry    SetSystemStateProgramming
                        ProgrammingEntry(fsm_var, okay_to_start_cycle);
                        if ( fsm_var->Transition == FALSE )
                        {
                            SetSystemStateProgramming(fsm_var, okay_to_start_cycle);
                        }
                        fsm_var->Transition = TRUE;
                    }
                    break;
                case FSM_STATE_PAUSE:
                    {
                        if ( !IsCancelCycleEnabled(okay_to_start_cycle) )
                        {
                            // PAUSE -->PROGRAMMING: evReset  [ !IsCancelCycleEnabled ] /  Reset   ResetTasks

                            // PAUSE :exit/   PauseExit
                            PauseExit(fsm_var, okay_to_start_cycle);
                            // transition action
                            Reset(fsm_var, okay_to_start_cycle);
                            ResetTasks(fsm_var, okay_to_start_cycle);
                            // define state being transitioned to
                            fsm_var->State = FSM_STATE_PROGRAMMING;
                            // PROGRAMMING :entry/   ProgrammingEntry    SetSystemStateProgramming
                            ProgrammingEntry(fsm_var, okay_to_start_cycle);
                            if ( fsm_var->Transition == FALSE )
                            {
                                SetSystemStateProgramming(fsm_var, okay_to_start_cycle);
                            }
                            fsm_var->Transition = TRUE;
                        }
                        else if ( IsCancelCycleEnabled(okay_to_start_cycle) )
                        {
                            // PAUSE -->CANCELLING: evReset  [ IsCancelCycleEnabled ] /  StartCancellingCycle   ResetTasks

                            // PAUSE :exit/   PauseExit
                            PauseExit(fsm_var, okay_to_start_cycle);
                            // transition action
                            StartCancellingCycle(fsm_var, okay_to_start_cycle);
                            ResetTasks(fsm_var, okay_to_start_cycle);
                            // define state being transitioned to
                            fsm_var->State = FSM_STATE_CANCELLING;
                            // CANCELLING :entry/   CancellingEntry    SetSystemStateCancelling
                            CancellingEntry(fsm_var, okay_to_start_cycle);
                            if ( fsm_var->Transition == FALSE )
                            {
                                SetSystemStateCancelling(fsm_var, okay_to_start_cycle);
                            }
                            fsm_var->Transition = TRUE;
                        }
                    }
                    break;
                case FSM_STATE_PROGRAMMING:
                    {
                        // PROGRAMMING -->PROGRAMMING: evReset  / Reset

                        // transition action
                        Reset(fsm_var, okay_to_start_cycle);
                        // define state being transitioned to
                        fsm_var->State = FSM_STATE_PROGRAMMING;
                        // PROGRAMMING :entry/   ProgrammingEntry    SetSystemStateProgramming
                        ProgrammingEntry(fsm_var, okay_to_start_cycle);
                        if ( fsm_var->Transition == FALSE )
                        {
                            SetSystemStateProgramming(fsm_var, okay_to_start_cycle);
                        }
                        fsm_var->Transition = TRUE;
                    }
                    break;
                case FSM_STATE_PAUSE_CANCELLING:
                    {
                        // PauseCancelling -->PROGRAMMING: evReset /  Reset   ResetTasks

                        // PauseCancelling :exit /   PauseExit
                        PauseExit(fsm_var, okay_to_start_cycle);
                        // transition action
                        Reset(fsm_var, okay_to_start_cycle);
                        ResetTasks(fsm_var, okay_to_start_cycle);
                        // define state being transitioned to
                        fsm_var->State = FSM_STATE_PROGRAMMING;
                        // PROGRAMMING :entry/   ProgrammingEntry    SetSystemStateProgramming
                        ProgrammingEntry(fsm_var, okay_to_start_cycle);
                        if ( fsm_var->Transition == FALSE )
                        {
                            SetSystemStateProgramming(fsm_var, okay_to_start_cycle);
                        }
                        fsm_var->Transition = TRUE;
                    }
                    break;
                case FSM_STATE_RUN:
                    {
                        if ( IsCancelCycleEnabled(okay_to_start_cycle) )
                        {
                            // RUN -->CANCELLING: evReset  [ IsCancelCycleEnabled ] /  StartCancellingCycle   ResetTasks

                            // RUN :exit/   RunExit
                            RunExit(fsm_var, okay_to_start_cycle);
                            // transition action
                            StartCancellingCycle(fsm_var, okay_to_start_cycle);
                            ResetTasks(fsm_var, okay_to_start_cycle);
                            // define state being transitioned to
                            fsm_var->State = FSM_STATE_CANCELLING;
                            // CANCELLING :entry/   CancellingEntry    SetSystemStateCancelling
                            CancellingEntry(fsm_var, okay_to_start_cycle);
                            if ( fsm_var->Transition == FALSE )
                            {
                                SetSystemStateCancelling(fsm_var, okay_to_start_cycle);
                            }
                            fsm_var->Transition = TRUE;
                        }
                        else if ( !IsCancelCycleEnabled(okay_to_start_cycle) )
                        {
                            // RUN -->PROGRAMMING: evReset  [ !IsCancelCycleEnabled ] /  Reset   ResetTasks

                            // RUN :exit/   RunExit
                            RunExit(fsm_var, okay_to_start_cycle);
                            // transition action
                            Reset(fsm_var, okay_to_start_cycle);
                            ResetTasks(fsm_var, okay_to_start_cycle);
                            // define state being transitioned to
                            fsm_var->State = FSM_STATE_PROGRAMMING;
                            // PROGRAMMING :entry/   ProgrammingEntry    SetSystemStateProgramming
                            ProgrammingEntry(fsm_var, okay_to_start_cycle);
                            if ( fsm_var->Transition == FALSE )
                            {
                                SetSystemStateProgramming(fsm_var, okay_to_start_cycle);
                            }
                            fsm_var->Transition = TRUE;
                        }
                    }
                    break;
            }
            break;
        case FSM_EVENT_RESUME: // event evResume
            switch ( fsm_var->State )
            {
                default:
                    // do nothing
                    break;
                case FSM_STATE_DELAY_PAUSE:
                    {
                        // DELAY_PAUSE --> DELAY : evResume

                        // DELAY_PAUSE :exit /   DelayPauseExit
                        DelayPauseExit(fsm_var, okay_to_start_cycle);
                        // define state being transitioned to
                        fsm_var->State = FSM_STATE_DELAY;
                        // DELAY :entry/   DelayEntry    SetSystemStateDelay
                        DelayEntry(fsm_var, okay_to_start_cycle);
                        if ( fsm_var->Transition == FALSE )
                        {
                            SetSystemStateDelay(fsm_var, okay_to_start_cycle);
                        }
                        fsm_var->Transition = TRUE;
                    }
                    break;
                case FSM_STATE_PAUSE:
                    {
                        // PAUSE --> RUN : evResume  / Resume

                        // PAUSE :exit/   PauseExit
                        PauseExit(fsm_var, okay_to_start_cycle);
                        // transition action
                        Resume(fsm_var, okay_to_start_cycle);
                        // define state being transitioned to
                        fsm_var->State = FSM_STATE_RUN;
                        // RUN :entry/   RunEntry    SetSystemStateRun
                        RunEntry(fsm_var, okay_to_start_cycle);
                        if ( fsm_var->Transition == FALSE )
                        {
                            SetSystemStateRun(fsm_var, okay_to_start_cycle);
                        }
                        fsm_var->Transition = TRUE;
                    }
                    break;
                case FSM_STATE_PAUSE_CANCELLING:
                    {
                        // PauseCancelling --> CANCELLING : evResume  / Resume

                        // PauseCancelling :exit /   PauseExit
                        PauseExit(fsm_var, okay_to_start_cycle);
                        // transition action
                        Resume(fsm_var, okay_to_start_cycle);
                        // define state being transitioned to
                        fsm_var->State = FSM_STATE_CANCELLING;
                        // CANCELLING :entry/   CancellingEntry    SetSystemStateCancelling
                        CancellingEntry(fsm_var, okay_to_start_cycle);
                        if ( fsm_var->Transition == FALSE )
                        {
                            SetSystemStateCancelling(fsm_var, okay_to_start_cycle);
                        }
                        fsm_var->Transition = TRUE;
                    }
                    break;
            }
            break;
        case FSM_EVENT_START: // event evStart
            switch ( fsm_var->State )
            {
                default:
                    // do nothing
                    break;
                case FSM_STATE_DELAY:
                    {
                        // DELAY -->RUN: evStart /  Start

                        // transition action
                        Start(fsm_var, okay_to_start_cycle);
                        // define state being transitioned to
                        fsm_var->State = FSM_STATE_RUN;
                        // RUN :entry/   RunEntry    SetSystemStateRun
                        RunEntry(fsm_var, okay_to_start_cycle);
                        if ( fsm_var->Transition == FALSE )
                        {
                            SetSystemStateRun(fsm_var, okay_to_start_cycle);
                        }
                        fsm_var->Transition = TRUE;
                    }
                    break;
                case FSM_STATE_PROGRAMMING:
                    {
                        // PROGRAMMING -->RUN: evStart /  Start

                        // transition action
                        Start(fsm_var, okay_to_start_cycle);
                        // define state being transitioned to
                        fsm_var->State = FSM_STATE_RUN;
                        // RUN :entry/   RunEntry    SetSystemStateRun
                        RunEntry(fsm_var, okay_to_start_cycle);
                        if ( fsm_var->Transition == FALSE )
                        {
                            SetSystemStateRun(fsm_var, okay_to_start_cycle);
                        }
                        fsm_var->Transition = TRUE;
                    }
                    break;
            }
            break;
    }
}

/**
 * @brief Define the SUPERVISOR_FSM_VAR_TYPE with the passed state argument
 * @param state = the current state of the state machine
 * @return = the SUPERVISOR_FSM_VAR_TYPE initialized with the state argument
 */
static SUPERVISOR_FSM_VAR_TYPE SupervisorBuildFsmVar(SUPERVISOR_FSM_STATE_TYPE state)
{
    SUPERVISOR_FSM_VAR_TYPE fsm_var;
    fsm_var.State = state;
    fsm_var.Transition = FALSE;
    return ( fsm_var );
}



#endif /* SUPERVISOR_FSM_H_ */
